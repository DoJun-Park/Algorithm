``` c++
#include <iostream>
#include <fstream>
#include <cstring>
#include <vector>
#include <utility>


using namespace std;

int n,m;
string str;
int lock_map[31][31];

pair<int, int> s;
pair<int, int> t;
int dfs_map[31][31]; //dfs번호

vector<pair<int, int>> node;
int cnt = 0;


void read_data(){
    ifstream ifs("lock.inp");
    string temp;
    ifs >> n>>m;


    for(int i=1; i<=n; ++i){
        ifs>>str;
        for(int j=1; j<=m; ++j){
            temp = str.at(j-1);
            if(temp.compare("_")==0){
                lock_map[i][j] = 0;
                continue;
            }
             else if(temp.compare("#")==0){
                lock_map[i][j] = 1;
                continue;
             }
             else if(temp.compare("S")==0){
                s.first = i;
                s.second = j;
                lock_map[i][j] = 5;
                 continue;
             }
             else{
                t.first = i;
                t.second = j;
                lock_map[i][j] = 27;
                 continue;
             }
         }
    }

    for(int i=1; i<=n; ++i){
        for(int j=1; j<=m; ++j){
            cout << lock_map[i][j]<<" ";
        }
        cout << endl;
    }

    ifs.close();
}

int dfs(pair<int,int> here, bool isRoot){

    dfs_map[31][31] =




}



int start(){
    pair<int,int> p;
    int a = s.first;
    int b = s.second;

    --a; //위로
    if(a != 0){
        if(lock_map[a][b] == 0 && dfs_map[a][b] == 0){
            p.first = a;
            p.second = b;
            dfs(p, true);
        }

    }

    a+=2; //아래로
    if(a != n){
        if(lock_map[a][b] == 0 && dfs_map[a][b] == 0){
            p.first = a;
            p.second = b;
            dfs(p, true);
        }
    }

    a= s.first;
    --b; //왼쪽
    if(b != 0){
        if(lock_map[a][b] == 0 && dfs_map[a][b] == 0){
            p.first = a;
            p.second = b;
            dfs(p, true);
        }
    }

    b+=2;
   if(b != m){
        if(lock_map[a][b] == 0 && dfs_map[a][b] == 0){
            p.first = a;
            p.second = b;
            dfs(p, true);
        }
    }









}




void write_data(){
    ofstream ofs("lock.out");
    ofs<<"  ";
    ofs.close();
}




int main(){
    read_data();
    start();
    write_data();
}



```
