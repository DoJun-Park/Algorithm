```c++
#include <iostream>
#include <cstdlib>
#include <fstream>

using namespace std;
int N;
int weight[21];
int sum= 0;

pair <int, int> location[21];



void read_data(){
    ifstream ifs("drone.inp");
    int in;
    ifs >> N;

    for(int i=0; i<N;  ++i){
        ifs>>in;
        ifs>>weight[in]>>location[in].first>>location[in].second;
        sum += weight[in];
    }

    ifs.close();
}

void sort_len(pair<int,int> order[], int in){

    pair<int, int> sort_order[21];

    int i=0;
    int chk = 0;

    cout << order[in].first<<endl;

    while(order[i].first != 0){
        if(i == in || order[i].first == 0){
            chk = 1;
            ++i;
        }
        else{
            if(chk == 1){
                sort_order[i-1].first = order[i].first;
                sort_order[i-1].second = abs(location[order[in].first].first - location[order[i].first].first) + abs(location[order[in].first].second - location[order[i].first].second);
                ++i;
            }
            else{
                sort_order[i].first = order[i].first;
                sort_order[i].second = abs(location[order[in].first].first - location[order[i].first].first) + abs(location[order[in].first].second - location[order[i].first].second);
                ++i;
            }
        }
    }



//    for(int i=0; i<N; ++i){
//        cout << "ccccfi :" << sort_order[i].first<< ", cccccccse: " << sort_order[i].second << endl;
//    }

    for(int i=0; i <N; ++i){
        for(int j=i; j<N; ++j){
            if(sort_order[i].second > sort_order[j].second){
                if(sort_order[i].first == 0 || sort_order[j].first == 0) ;
                else{
                    int temp = sort_order[i].first;
                    sort_order[i].first = sort_order[j].first;
                    sort_order[j].first = temp;

                    temp = sort_order[i].second;
                    sort_order[i].second = sort_order[j].second;
                    sort_order[j].second = temp;

                }
            }
        }
    }

    int j=0;

    while(sort_order[j].first != 0){
        order[j].first = sort_order[j].first;
        order[j].second = sort_order[j].second;
        ++j;
    }

    for(int i=0; i<N; ++i){
        cout << "order fi :" << order[i].first<< ", order se: " << order[i].second << endl;
    }

    for(int i=0; i<N; ++i){
        cout << "fi :" << sort_order[i].first<< ", se: " << sort_order[i].second << endl;
    }


    cout << endl;

}


void shortest_path(pair<int,int> order[], int energy, int in){

    if(order[0].first == 0){
        //계산하고 return
    }

    else{
       sort_len(order, in);


//          for(int i=0; i<N; ++i){
//        cout << "요기 fi :" << order[i].first<< ",ㅇ요기 se: " << order[i].second << endl;
//    }

    cout << endl;




    }

}

void first(){
    pair<int, int> first_order[21];
    int first_energy;


    for(int i=2; i<=N; ++i){
        first_order[i-2].first = i;
        first_order[i-2].second = abs(location[1].first - location[i].first) + abs(location[1].second - location[i].second);
    }

    for(int i=0; i<N; ++i){
        for(int j=i; j<=N; ++j){
            if(first_order[i].second > first_order[j].second){
                if(first_order[i].first == 0 || first_order[j].first == 0) continue;
                else{
                    int temp = first_order[i].first;
                    first_order[i].first = first_order[j].first;
                    first_order[j].first = temp;

                    temp = first_order[i].second;
                    first_order[i].second = first_order[j].second;
                    first_order[j].second = temp;
                }
            }
        }
    }

    for(int i=0; i<N; ++i){
        cout << "111fi :" << first_order[i].first<< ", 111se: " << first_order[i].second << endl;
    }

    cout << endl;

    for(int i=0; i<N; ++i){
      if(first_order[i].first == 0) return;

      else{
        first_energy = (sum+1)*first_order[i].second;
        shortest_path(first_order, first_energy,i);
      }

    }

    return;

}







void write_data(){
    ofstream ofs("drone.out");
    ofs << "d";
    ofs.close();
}



int main(){
    read_data();
    first();
    write_data();


}

```
